# Domains

Domain modules capture reusable business language and capabilities, such as
retail checkout or banking transfers. They must not contain application URLs,
credentials, or selectors that belong to one project.
