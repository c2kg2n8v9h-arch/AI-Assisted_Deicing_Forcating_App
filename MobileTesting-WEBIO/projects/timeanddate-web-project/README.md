# Timeanddate Web Project

This project contains Timeanddate date-duration automation tests and project-local RAG governance. Retrieval must stay scoped to Timeanddate project knowledge first, then reusable date-calculation domain knowledge, then approved platform knowledge.

Production execution is explicitly configured in `run.mjs`; confirm target and test-data safety before live runs.
