# Shared packages

Shared packages contain contracts, configuration, reporting, observability,
security, test-data abstractions, optional AI capabilities, and provider or
technology adapters. Package boundaries will be enforced through TypeScript
project references and workspace dependencies as implementations are added.
