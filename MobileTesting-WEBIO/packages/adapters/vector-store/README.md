# Vector-store adapters

Implement `VectorStorePort` here. Document tenancy, encryption, retention,
deletion, residency, backup, and access control before ingestion. Generated
indexes are runtime data and must not be committed.
