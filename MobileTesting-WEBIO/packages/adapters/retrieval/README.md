# Retrieval adapters

Implement `EmbeddingPort` and/or `RetrieverPort` here. Enforce the request's
classification ceiling and return source citations. Documents without approved
provenance are not eligible for ingestion.
