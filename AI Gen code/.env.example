# Local development only. This grants read-only access and must never be used in production.
DEICING_LOCAL_MODE=false
DEICING_ENV=development

# Production: inject DEICING_USERS through a secret manager or private environment.
# Format: a JSON list of objects containing username, role, api_key, and a
# non-empty station_codes list, for example ["DEN"] or ["*"].
# Do not place real credentials in this file or commit them to source control.
