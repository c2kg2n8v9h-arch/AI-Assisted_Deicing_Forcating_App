# Raw sources

Store original, unchanged source files here, grouped by collection. Each source
must have provenance and a SHA-256 checksum in its manifest. Raw files are not
automatically approved for operational retrieval.
